    - name: upgrade packages
      pacman: upgrade=yes update_cache=yes

    - name: packages 
      package: name={{item}} state=present
      with_items:
           - cmake
           - pkg-config
           - yajl
           - base-devel
           - gcc-fortran
           - r
   

